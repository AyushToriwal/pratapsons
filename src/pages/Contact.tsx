import { useState, type FormEvent } from "react";
import { ExternalLink, MapPin, MessageCircle, Phone } from "lucide-react";
import { Link } from "react-router-dom";
import { Button } from "@/components/ui/button";
import { Input } from "@/components/ui/input";
import { Label } from "@/components/ui/label";
import { Textarea } from "@/components/ui/textarea";
import SeoHead from "@/components/site/SeoHead";
import { PageLayout, SectionHeading } from "@/components/site/SiteLayout";
import { SERVICE_AREAS, SITE_CONFIG } from "@/lib/site";

export default function Contact() {
  const [name, setName] = useState("");
  const [phone, setPhone] = useState("");
  const [service, setService] = useState("");
  const [message, setMessage] = useState("");
  const [whatsappFallbackUrl, setWhatsappFallbackUrl] = useState<string | null>(null);

  const handleSubmit = (event: FormEvent<HTMLFormElement>) => {
    event.preventDefault();
    const whatsappMessage = [
      "Hello Pratap Sons, I would like to request a service.",
      `Name: ${name.trim()}`,
      `Phone: ${phone.trim()}`,
      `Service: ${service.trim()}`,
      `Location / message: ${message.trim()}`,
      "Enquiry source: Website contact page",
    ].join("\n");
    const whatsappUrl = `https://wa.me/918077556596?${new URLSearchParams({ text: whatsappMessage }).toString()}`;
    setWhatsappFallbackUrl(whatsappUrl);
    window.open(whatsappUrl, "_blank", "noopener,noreferrer");
  };

  return <PageLayout><SeoHead title="Contact Pratap Sons AC & Water Services in Noida" description="Call or send a service enquiry to Pratap Sons for AC repair, water tanker delivery, geyser and heater repair in Noida." path="/contact" /><section className="border-b border-slate-200 bg-[#f1f5f3]" data-testid="contact-hero"><div className="mx-auto max-w-7xl px-4 py-14 sm:px-6 md:py-20 lg:px-8"><p className="text-xs font-bold uppercase tracking-[0.2em] text-teal-800" data-testid="contact-eyebrow">Contact & service areas</p><h1 className="mt-4 max-w-3xl font-heading text-4xl font-extrabold tracking-tight text-slate-950 sm:text-5xl" data-testid="contact-h1">Tell us what service you need in Noida</h1><p className="mt-5 max-w-2xl text-base leading-7 text-slate-600" data-testid="contact-intro-copy">Call, WhatsApp or send a short enquiry. This form is frontend-only and will show a confirmation on this page.</p></div></section>
    <section className="py-16" data-testid="contact-main-section"><div className="mx-auto grid max-w-7xl gap-12 px-4 sm:px-6 md:grid-cols-[0.75fr_1.25fr] lg:px-8"><div><SectionHeading eyebrow="Reach Pratap Sons" title="Choose the easiest way to get in touch" text="Keep your location, appliance or water requirement ready when you contact us." /><div className="mt-8 space-y-3" data-testid="contact-detail-list"><a href={SITE_CONFIG.phoneUri} className="flex items-start gap-4 border border-slate-200 bg-white p-5 hover:border-teal-300" data-testid="contact-phone-card"><Phone className="mt-0.5 size-5 text-teal-800" /><span><span className="block text-xs font-bold uppercase tracking-[0.16em] text-slate-500">Call Now</span><span className="mt-1 block font-heading font-bold text-slate-950">{SITE_CONFIG.phone}</span></span></a><a href={SITE_CONFIG.whatsappUri} className="flex items-start gap-4 border border-slate-200 bg-white p-5 hover:border-emerald-300" data-testid="contact-whatsapp-card"><MessageCircle className="mt-0.5 size-5 text-emerald-700" /><span><span className="block text-xs font-bold uppercase tracking-[0.16em] text-slate-500">WhatsApp</span><span className="mt-1 block font-heading font-bold text-slate-950">{SITE_CONFIG.whatsapp}</span></span></a><a href={SITE_CONFIG.mapsUrl} target="_blank" rel="noreferrer" className="flex items-start gap-4 border border-slate-200 bg-white p-5 hover:border-teal-300" data-testid="contact-address-card"><MapPin className="mt-0.5 size-5 text-teal-800" /><span><span className="block text-xs font-bold uppercase tracking-[0.16em] text-slate-500">Address / map</span><span className="mt-1 block font-heading font-bold text-slate-950">{SITE_CONFIG.address}</span></span><ExternalLink className="ml-auto size-4 text-slate-400" /></a></div><div className="mt-8 flex flex-wrap gap-2" data-testid="contact-service-area-list">{SERVICE_AREAS.map((area, index) => <span key={area} className="border border-slate-300 bg-[#f1f5f3] px-3 py-2 text-xs font-semibold text-slate-700" data-testid={`contact-service-area-${index + 1}`}>{area}</span>)}</div></div>
      <div className="border border-slate-200 bg-white p-6 sm:p-8" data-testid="contact-form-card"><div className="flex items-start justify-between gap-6"><div><p className="text-xs font-bold uppercase tracking-[0.18em] text-teal-800" data-testid="contact-form-eyebrow">Request service</p><h2 className="mt-3 font-heading text-2xl font-extrabold text-slate-950" data-testid="contact-form-title">Prepare a WhatsApp enquiry</h2></div><span className="hidden border border-emerald-200 bg-emerald-50 px-3 py-2 text-xs font-bold text-emerald-800 sm:block" data-testid="contact-form-note">No details stored</span></div><p className="mt-4 text-sm leading-6 text-slate-600" id="contact-privacy-note" data-testid="contact-privacy-note">Your details stay in this browser and are placed into a WhatsApp draft for you to review before sending.</p><form className="mt-8 space-y-5" onSubmit={handleSubmit} aria-describedby="contact-privacy-note" data-testid="contact-form"><div className="grid gap-5 sm:grid-cols-2"><div className="space-y-2"><Label htmlFor="contact-name" data-testid="contact-name-label">Name</Label><Input id="contact-name" value={name} onChange={(event) => setName(event.target.value)} placeholder="Your name" autoComplete="name" minLength={2} required data-testid="contact-name-input" /></div><div className="space-y-2"><Label htmlFor="contact-phone" data-testid="contact-phone-label">Phone number</Label><Input id="contact-phone" type="tel" inputMode="tel" value={phone} onChange={(event) => setPhone(event.target.value)} placeholder="Your phone number" autoComplete="tel" minLength={7} maxLength={20} required data-testid="contact-phone-input" /></div></div><div className="space-y-2"><Label htmlFor="contact-service" data-testid="contact-service-label">Service needed</Label><Input id="contact-service" value={service} onChange={(event) => setService(event.target.value)} placeholder="AC repair, water tanker, geyser repair..." minLength={2} required data-testid="contact-service-input" /></div><div className="space-y-2"><Label htmlFor="contact-message" data-testid="contact-message-label">Location and message</Label><Textarea id="contact-message" value={message} onChange={(event) => setMessage(event.target.value)} placeholder="Sector, landmark and a short description" rows={5} minLength={3} required data-testid="contact-message-input" /></div><Button type="submit" className="min-h-12 w-full bg-emerald-600 text-white hover:bg-emerald-700" data-testid="contact-form-submit-button"><MessageCircle className="size-5" /> Continue on WhatsApp</Button>{whatsappFallbackUrl && <p className="border border-emerald-200 bg-emerald-50 p-4 text-sm leading-6 text-slate-700" role="status" data-testid="contact-whatsapp-fallback">WhatsApp should open with your completed enquiry. If it did not, <a href={whatsappFallbackUrl} target="_blank" rel="noopener noreferrer" className="font-bold text-emerald-800 underline underline-offset-4" data-testid="contact-whatsapp-fallback-link">open the prepared message manually</a>.</p>}</form></div></div></section>
    <section className="border-y border-slate-200 bg-[#f1f5f3] py-16" data-testid="contact-map-section"><div className="mx-auto max-w-7xl px-4 sm:px-6 lg:px-8"><div className="grid gap-8 md:grid-cols-[0.75fr_1.25fr] md:items-center"><div><SectionHeading eyebrow="Noida service area" title="Local coverage, clear directions" text="Pratap Sons is based in Gijhore, Sector 53, Noida, and serves customers across Noida." /><p className="mt-5 flex items-start gap-2 text-sm font-semibold leading-6 text-slate-700" data-testid="contact-map-address"><MapPin className="mt-0.5 size-4 shrink-0 text-teal-800" />{SITE_CONFIG.address}</p><a href={SITE_CONFIG.mapsUrl} target="_blank" rel="noopener noreferrer" className="mt-5 inline-flex items-center gap-2 text-sm font-bold text-teal-800 hover:text-teal-950" data-testid="contact-map-link">Open in Google Maps <ExternalLink className="size-4" /></a></div><div className="overflow-hidden border border-slate-300 bg-white" data-testid="contact-map-card"><iframe title={`Map showing ${SITE_CONFIG.address}`} src={SITE_CONFIG.mapEmbedUrl} loading="lazy" referrerPolicy="strict-origin-when-cross-origin" allowFullScreen className="block h-80 w-full border-0 md:h-96" data-testid="contact-map-iframe" /></div></div></div></section>
    <section className="bg-[#0f292f] py-14 text-white" data-testid="contact-bottom-cta"><div className="mx-auto flex max-w-7xl flex-col gap-5 px-4 sm:px-6 md:flex-row md:items-center md:justify-between lg:px-8"><div><h2 className="font-heading text-2xl font-extrabold" data-testid="contact-bottom-cta-title">Prefer a direct conversation?</h2><p className="mt-2 text-sm text-slate-300" data-testid="contact-bottom-cta-copy">Call or WhatsApp with your Noida sector and service requirement.</p></div><Link to="/ac-services" className="inline-flex items-center justify-center border border-white/30 px-5 py-3 text-sm font-bold text-white hover:bg-white/10" data-testid="contact-ac-services-link">See AC services</Link></div></section>
  </PageLayout>;
}